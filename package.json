{
  "name": "lexitrain",
  "version": "1.0.0",
  "description": "LexiTrain — dictionary app with GPT + Vercel KV",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "build": "vercel build"
  },
  "dependencies": {
    "@vercel/kv": "latest"
  }
}
